function setup() {
    createCanvas(600,400);
    textAlign(CENTER,CENTER);
    textSize(40);
    fill("rgb(255,255,0)");
    yPos=height;
}

function draw() {
    hour=hour()
    minute=minute()
    
}